# Default Configs

`defaultConfigs` persists the application's default chat, fast, embedding, image, speech, transcription, and prompt-optimization model references. It also stores the prompt-optimization template consumed by the shared Conversation composer tool. Feature API availability is owned centrally by `Capabilities`; it is no longer stored as a default setting or character-package override.
