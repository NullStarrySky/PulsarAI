# Built-in documentation

`BuiltInDocsPage` is the in-app documentation surface registered in Settings.
It owns three chapters—design thinking, interface control and API—and selectable
pages within each chapter. Existing feature documents are imported as raw text
where useful rather than copied into another content database. The layout falls
back from three columns to compact chapter/page selectors on narrow screens.
