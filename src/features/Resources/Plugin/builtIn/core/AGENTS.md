# 内置插件

`generate.js` 是唯一生成入口。它显式读取上下文资源、选择压缩结果并创建 Agent；Conversation 不替它组装上下文。生成流程通过 `reply` 实时写入空助手目标，不以脚本返回值传递正文。内置 Agent 消费 `runner.stream()`：过程回调持续写入步骤，`text-delta.text` 逐段追加正文；开始下一轮 step 时替换上一轮临时正文，使最终消息保留最后一轮文本。流结束后可读取完整正文并覆盖为后处理结果。
