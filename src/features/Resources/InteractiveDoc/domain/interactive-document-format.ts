export const interactiveDocumentFormatPrompt = [
  "## Interactive Document (.imd)",
  "An `.imd` file is UTF-8 text using an SFC-like format.",
  "Wrap each message template in `<prompt_template name=\"...\" role=\"system|user|assistant\">...</prompt_template>`.",
  "Prompt templates may contain Markdown, Sandbox `{{...}}` expressions, `[[...]]` message splices, and explicit `<@...>` references.",
  "Wrap local data in one `<data>` block containing any number of `<sub_data name=\"...\">` blocks.",
  "Every `sub_data` contains `<enable_updater>`, `<description>`, `<content type=\"json|value\">`, and an optional `<wrapper>` function.",
  "When `enable_updater` is true, the value is an anemic, replayable variable. Its description is added to the model's variable-description context, and successful `codeAct` variable-update functions are bound to concrete message versions.",
  "A wrapper has the form `function (value, variable) { return facade; }`. Mutate JSON objects through facade methods; use `variable.replace(nextValue)` when replacing a primitive or the whole value.",
  "An optional `<memory><compression_threshold>N</compression_threshold></memory>` block enables hierarchical conversation compression. Zero or omission disables it; positive values are normalized to at least four containers.",
  "Access local data with `<@local:name>`.",
  "Use `<@container-name>` for a uniquely visible container. External resources can also use `<@path:./relative.md>`, `<@id:resource-id>`, or `<@container:root|plugin|global/name>`.",
  "Inside `{{...}}` or `[[...]]`, an explicit reference is compiled to a guarded `ref(...)` call.",
  "Declare containers in the owning plugin root `containers.xml` with `<container name=\"...\" scope=\"root|plugin|global\">`; nested `<include as=\"alias\">container:scope/name</include>` rows reference existing container namespaces without flattening them.",
  "Resource membership is file metadata managed by the Plugin workspace; it is not written into `.imd` content.",
  "Container values expose `get(alias)`, `use(alias)`, and `list()` and remain lazy namespaces.",
].join("\n");
