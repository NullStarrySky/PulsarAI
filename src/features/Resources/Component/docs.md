# Component Resources

Component resources keep Vue SFC source as plain text. The reserved preview component renders the source through Milkdown as fenced code instead of executing it, so future plugin work can inspect component assets without reopening the old dynamic SFC loader path.
