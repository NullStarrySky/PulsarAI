# PulsarAI Agent Guide

## Hard Rules

- Do not run `bun run build` unless the user explicitly asks for production packaging or build verification.
- Do not run full test suites unless the user explicitly asks.
- When reading files with PowerShell `Get-Content`, always pass `-Encoding UTF8`.
- Prefer scoped implementation, current feature seams, and small verifiable steps.
- When implementing UI components, reuse shadcn-vue components and theme CSS variables whenever practical so spacing, color, radius, focus, and state styling stay consistent.
- When implementing or changing components, explicitly consider narrow-window and mobile-platform behavior. Use the shared responsive state instead of scattering viewport or platform checks, keep touch targets usable, and provide an explicit layout fallback below 768px.
- Commands that expose feature behavior to search or hotkeys should keep their implementation in the owning feature's `actions.ts`; UI may register and route commands but should not own domain behavior.
- Plugin files are owned by `src/features/Resources/Plugin`; keep plugin list/workspace/tree controls there and do not reintroduce a generic plugin center page under `src/features/UI`. Each character package owns exactly one package-local resource plugin through `pluginId`, selects one local or global `mainPluginId` for both root context and generation process, and stores an unordered `enabledGlobalPluginIds` activation set. Plugins with `packageId: null` are global and retain their descriptive metadata; package-local plugin metadata remains persisted but hidden in normal UI. System plugins additionally use `builtIn`, remain editable, and expose restore-to-default instead of write locks. Plugin order and `main` flags must not be reintroduced; collisions are explicit errors, while equal-priority resources use stable IDs and paths only as deterministic tie breakers.
- Conversation generation keeps a fixed startup sequence in `Resources/Conversation/application/conversation-generation.ts`: create the blank assistant target, build the minimal authorized runtime environment, index Plugin containers, resource-metadata `.data` references, and explicit document references, compile the root `context.md`, then hand control to root `agentprocess/index.js`. Agent exposes a lazy `agent.prepare()` resource provider, while the visible built-in process owns `new ToolLoopAgent(...)`; custom non-Agent flows must not hydrate a model, and no implicit default-Agent fallback may be restored.
- Updater-enabled `.data` definitions produce replayable Conversation state. Keep isolation, the anemic initial value, wrapper, and description inside `.data`; keep `{ alias, dataId }` references only on resource metadata. Bind successful deterministic update functions in original order to the concrete assistant `ChatMessage.meta.variableUpdate`, replay only the active message-version path, and use bounded in-memory state-version caches rather than persisted per-message snapshots. Resource-isolated instances are addressed by resource ID while conversation-isolated instances share one state per conversation. `codeAct` remains the only model tool; its synchronous `variable-update` intent exposes transactional `data.readForResource(resourceId, dataId)` / `data.writeForResource(resourceId, dataId, value)` without Feature/Plugin APIs, network/files, detached async work, current time, randomness, or other side effects, returns failures for model repair, and throws after three consecutive failures.
- Compression memory is derived immutable Conversation data configured by the root `context.md` resource metadata threshold. Keep recent containers raw, persist leaf pointers with exact container/message-version identities, allow higher pointers to index adjacent lower pointers, validate the complete pointer chain against the active path, and fall back to raw chat with diagnostics on compression failure. Do not rewrite message content or branch topology; delete derived segments with their conversation.
- Conversation message failures use `ChatMessage.type: "error"` rather than a new AI role. Keep them visible and persisted, render them as destructive alerts, exclude them from both `chat` and generation `activePath`, and expose explicit insertion through the authorized `conversation.pushErrorMessage(content)` capability.
- Message favorites belong to concrete `ChatMessage` versions. Keep map search scoped to the current conversation, rank favorite matches first, and route both map results and the Conversation-owned favorite settings page through the shared version/branch activation and renderer-jump path.
- Conversation reasoning depth persists on `Conversation.reasoningEffort` with `none`, `low`, `medium`, `high`, and `xhigh`. Expose it to the Sandbox as `reasoningEffort`, and pass it to model calls through AI SDK 7's top-level `reasoning` option; keep the composer control in the shared toolbar catalog.
- Feature API test injection persists per Conversation through `featureApiEnabled` and defaults to enabled. Keep its toggle in the shared composer toolbar catalog; when disabled, omit both the Capability runtime objects and generated Feature API prompt instead of hiding documentation while leaving APIs callable.
- The standard conversation list uses dynamic-height TanStack virtualization. Keep logical message counts and total virtual height visible to `MessageScroller`, measure every mounted row, and route off-screen message jumps through the virtualizer before relying on DOM lookup.
- Interactive chat code previews are controlled by the persisted Appearance setting `interactiveCodePreview` (default off). When enabled, only recognize fenced blocks containing `<html>`, `<!DOCTYPE>`, or `<script>`, run them in `srcdoc` iframes without `allow-same-origin`, replace the latest active message body with its preview, and preserve a source-view escape hatch.
- Reversible translation metadata belongs to the concrete `ChatMessage` version under `meta.translation`. Clear stale translation snapshots on manual edits and restore through the Conversation store rather than presentation-only state.
- Conversations always belong to a user character package and use `Conversation.kind` (`chat`, `task`, or `test`) for hard semantics. Persist resource relationships in `Conversation.binding`; do not introduce a system character package as a storage bucket. Task conversations with a package-bound resource use `Agent/application/project-agent-runtime.ts`.
- Keep conversation and task controls in the right sidebar. The plugin list belongs in the conversation left sidebar and opens plugins as independent workspace pages. The merged `任务` panel derives its binding from the active workspace resource, includes the current resource, owning package, and relevant Feature guide in generation context, and manages associated task/test conversation files from its header. It uses the shared conversation composer. Plugin tests are database conversations with `kind: "test"` and a plugin binding, never message-history files inside the plugin tree.
- Conversation renderers are selected per `Conversation` through `rendererId`. Keep renderer UI under `Resources/Conversation/presentation`; templates persist this field and new conversations inherit it when their message path is cloned.
- Plugin contents are a nested `root` file tree. Derive ordinary file semantics from suffixes, but select convention-file editors and runtime roles by exact root filename. Use `info.md` as documentation, `manifest.json` as the single plugin configuration file with a `PluginManifestGroupContent[]` root, `containers.json` for JSON container topology, root `regex.json` for ordered context/render post-processing, `context.md` as the role-aware Markdown entry document, `.data` for reusable JSON data definitions, `agentprocess/index.js` as the only plugin process entry, `tools/<name>/tool.js + prompt.md` for CodeAct-callable custom functions and their model documentation, `Override.vue` as the default conversation-renderer override, and `components/` for reusable plugin Vue components. Manifest values are addressed by `group.id/content.id`; built-in component names map to singleton shadcn wrappers and plugin components use the template-only Vue runtime. Markdown uses the Milkdown editing surface without a raw-source mode; raw/preview switching is reserved for Vue and convention JSON files with structured renderers. Vue preview is template-only and must not execute plugin `<script>`. Do not add implicit fallback entries or automatic plugin-tree rewrites.
- Plugin resources never become ambient Sandbox variables. Cross-resource access must use `<@container-name>` for an unambiguous visible container or explicit `<@path:...>`, `<@id:...>`, `<@container:root|plugin|global/name>`, `<@config:local/group/content>`, and `<@config:global/pluginId/group/content>` references. Container declarations and namespaced container references belong to root `containers.json`; member relationships and their optional local manifest conditions persist on `PluginFile.memberships` metadata, while `.data` bindings persist on `PluginFile.dataReferences`, never in resource content. Numeric depth containers are derived only from `PluginFile.contextPlacement.depth`: K is a non-negative integer, zero appends after the base message list, anchors are computed before insertion, and names must be unique within one K. Containers remain lazy namespaces and must report collisions, missing references, invalid JSON, and cycles instead of applying last-write-wins merges.
- Plugin container declarations may include an optional description. Keep it serialized in root `containers.json`, editable in the filename-specific container editor, and available to Markdown `<@...>` completion together with scope and owning-plugin context; descriptions are authoring metadata and must not be injected into generation automatically.
- Container inspection and generic reading must reuse `plugin-reference-resolver.ts` so the compact `containers.json` details UI, `ctx.containers`, and the Plugin capability report the same enabled topology. Containers own no extractor, transformer, template, Skill adapter, or other execution policy. Keep direct-use documents and resolved contents navigable through `listContainers`, `getContainer`, `listContainerContents`, and `readContainer`, retaining IDs, paths, sources, and priorities. Container CRUD stays plugin-process-scoped; depth placement mutates only file `contextPlacement` metadata and does not create a declaration or membership.
- Plugin files persist an independent numeric `priority`, defaulting to `100`. Higher-priority members enter resolved containers first while equal priorities use stable plugin IDs and resource path/ID keys rather than a configurable plugin order.
- Plugin background files use media content (`kind: "media"`) with an image or video URL and live under the conventional `background/` folder. All media descendants are available by convention; keep media parsing, preview, and built-in background assets under `Resources/Plugin` rather than encoding backgrounds as CSS snippets.
- Conversation actions are JavaScript or Markdown files under the conventional Plugin `action/` folder. JavaScript commands keep the leading `ActionPart`, add `action` and `prompt` to the minimal Sandbox environment, and temporarily replace `agentprocess/index.js`; Markdown commands only replace the composer draft with their file content and never create a message by themselves.
- Enabled root `regex.json` files form the ordered regex container. Sort files by descending `PluginFile.priority` and stable plugin/resource IDs, then apply their rules to the final role-preserving generation messages using one-based depth from the end. Conversation rendering reuses the same ordered rules but only those with `applyOnRending: true`; never persist rendered replacements back into message content.
- `codeAct` is the only model-visible tool. It must accept one JavaScript function with an explicit `return`, execute it against the authorized Sandbox environment, and return either the function value or a structured error to the model. Keep Feature APIs, Plugin APIs, ask-user, Skill, and MCP operations as callable context functions instead of adding parallel AI SDK tools.
- Plugin custom tools remain ordinary Sandbox functions, never additional AI SDK tools. Discover only immediate `tools/<name>/tool.js` functions that also have `prompt.md`, order them by descending `tool.js` priority and stable plugin/resource keys, reject name collisions, expose them as `ctx.tools[name](...args)`, and inject one `# 自定义工具` system block containing each prompt plus plugin/resource IDs and paths.
- Skill and MCP integrations remain registered through `Agent/application/agent-extension-registry.ts`, but CodeAct calls them through context APIs such as `agent.callExtension(...)`, `skills.call(...)`, or `mcp.call(...)`; do not create a separate conversation generation path for either feature.
- Ask-user input and normalization are owned by `Agent/application/ask-user-tool.ts` and pause CodeAct through the Conversation generation-component requester. Keep question UI registered as `agent.ask-user`, and expose it through `agent.askUser(...)` / `api.askUser(...)` instead of a second model tool or parallel chat protocol.
- Role-aware context documents are owned by `src/features/Resources/InteractiveDoc`. Persist them as ordinary UTF-8 Markdown; use non-nesting `:::pulsar role=system|user|assistant` fences only when a role must be explicit, and delegate `{{}}` / `[[ ]]` evaluation to the existing Sandbox after explicit references are linked. Leading YAML frontmatter must round-trip through the shared Milkdown feature without entering rendered document content; plugin Markdown keeps a source/Milkdown switch so authors can edit that metadata. Data definitions live in separate `.data` JSON files, declare `resource` or `conversation` isolation internally, and are referenced only by stable IDs plus aliases in `PluginFile.dataReferences` metadata.
- Version management is owned by `src/features/Backup`. Keep full-database disaster restore separate from selective resource restore, store new history snapshots as manifests over the shared SHA-256/Zstandard object repository while retaining legacy directory-backup reads, and remap parent and child IDs together when importing resource copies. Portable package, conversation, and plugin archives use stable IDs; update mode must diff and merge domain relationships while preserving conflicting message versions and plugin files. Keep LAN version metadata in `src/features/Database/application/sync-metadata.ts`.
- When implementing settings forms, reuse `SettingForm` and `SettingFormField` from `src/features/Setting/presentation/` whenever practical so each field has a clear title, description, and control area.
- Feature APIs exposed to generated JavaScript must be defined in the owning feature's `capabilities.ts`. Keep human `documentation`, documented types, `subCaps`, builder methods, API signatures/descriptions/examples, default or package grants, and behavior synchronized. `src/features/Capabilities` is the aggregation/runtime owner; VitePress must generate its human Markdown, outline, and model-facing preview from these definitions instead of maintaining a second API inventory.
- Capability definitions that must also be consumed without loading their runtime store dependencies may live in the owning Feature's `domain/` module and be re-exported from `capabilities.ts`. Project Agent context must generate Plugin API documentation from that shared definition instead of copying signatures into its prompt.
- Conversation composer toolbar placement is owned by the UI appearance store as normalized `left`, `right`, and `unused` arrays. New composer tools must be added to the shared toolbar catalog and receive a migration/default placement instead of being hard-coded into the workspace footer.
- The Conversation composer `map` tool renders the current conversation's `ChatMessageContainer` tree without merge edges. Keep graph layout and node navigation under `Resources/Conversation`, activate every ancestor branch before jumping, and use the shared toolbar catalog for placement.
- Native and built-in notifications go through `features/Notification/application/notification-service.ts`; external delivery is the default, while `channel: "internal"` writes to the built-in notification center.
- Top-bar tab status is generic UI state exposed by `layout-store.ts`. Feature code may set status for its own resource tab, but the UI shell owns status rendering.
- Main-window close behavior is generic UI lifecycle state owned by `UI/application/window-lifecycle-store.ts`. Intercept close requests only for the `main` window; subwindows close normally. The native desktop tray only restores/focuses the main window on left click and exits from its context menu.
- `todo/main.todo` is the backlog index and references focused plans in the same folder. Treat them as project context when planning feature work, but do not delete completed or skipped items unless explicitly asked.

## Project Shape

Pulsar is an open LLM frontend built with Tauri, TypeScript, Vue 3, shadcn-vue, AI SDK, SurrealDB, and Milkdown.

The frontend follows a DDD-inspired feature layout under `src/features`. Each feature should own its domain types, application services, infrastructure adapters, and presentation components when those layers become necessary.

`src/features/UI` owns only shell, tab, sidebar host, and generic resource-hosting mechanics. Feature-specific resource pages and sidebar contents must live in their owning feature and register into UI through the resource/sidebar registries.

## Current Phase

The app is moving through the basic agent and conversation-runtime phase:

- one minimal conversation shell;
- model provider settings and default model selection;
- local SurrealDB-backed settings and secrets;
- AI SDK wrappers that hydrate provider/model references;
- an editable built-in plugin workflow that constructs AI SDK `ToolLoopAgent` from Agent-provided model/tools;
- a frontend sandbox for macro expansion and controlled JavaScript execution;
- Feature-level capability builders that assemble the default Sandbox API environment and inject the same API reference into model context and VitePress;
- appearance, font, and about settings;
- Milkdown/Crepe markdown rendering for messages and composer input;
- plugin file trees as normal workspace resources with scoped lazy containers, explicit references, and no duplicated composer selector;
- role-aware Markdown documents plus metadata-bound reusable `.data`, Milkdown editing, and compiled context messages;
- built-in workspace pages opened through the UI resource registry rather than sidebar-local mode switching;
- SubWindow protocol and WebviewWindow adapters isolated under `src/features/SubWindow`;
- `design.md` as the project index.

MCP, Skill, and richer tool orchestration are later-phase additions.
