# PulsarAI 会话记忆系统设计讨论

> 状态：第一版已实现，本文继续作为完整设计与后续演进依据。
>
> 本文记录条目式记忆与压缩式记忆的完整设计讨论。第一版已经实现 IMD 贫血值/包装器/说明/压缩阈值、消息版本绑定更新、激活路径重放、更新节点内存缓存、叶区间摘要与递归摘要指针。数据库容器、持久检查点、Worker 冷重放、下探检索 Agent、完整 MVCC 与可视化诊断仍是后续阶段；文中的其它候选类型名和目录名不一定已经落地。

## 1. 背景

记忆系统是会话系统的主要复杂度来源之一。

PulsarAI 的会话并不是一段扁平消息列表，而是一棵包含消息版本和后续分支的容器树：

- `ChatMessageContainer` 表示会话拓扑中的一个节点；
- 一个容器的 `content` 可以保存多个 `ChatMessage` 版本；
- `activeMessage` 选择容器当前使用的消息版本；
- `previousContainer`、`availableNextContainer` 和 `activeNextContainer` 构成无合并的分支结构；
- 生成、渲染和记忆都必须以当前激活路径及其选中的消息版本为依据。

记忆系统包含两类语义不同但需要协同工作的能力：

1. **条目式记忆**：把消息版本绑定的更新函数沿激活路径重放，计算当前世界状态、人物设定、JSON 对象或数据库容器状态。
2. **压缩式记忆**：把连续的静态消息容器压缩为可递归引用的摘要区间，减少送入模型的原始上下文，并允许按需向下查找细节。

二者分别回答：

- 条目式记忆：“世界现在是什么状态？”
- 压缩式记忆：“过去发生了什么，当前模型需要知道多少？”

二者不应合并成同一种数据结构，也不应共享同一种跳跃语义。

## 2. 总体原则

### 2.1 消息、状态和摘要的事实来源不同

- 原始消息版本是会话内容的事实来源。
- 条目式记忆的更新函数是状态变化的事实来源。
- 条目式记忆计算出的对象版本是派生结果。
- 压缩摘要是原始消息区间的有损派生结果。
- 缓存、内部写日志、摘要索引和临时模型消息都不是新的事实来源。

### 2.2 分支与消息版本必须进入记忆身份

同一容器切换 `activeMessage` 后，即使容器 ID 不变，条目状态和压缩摘要的来源都可能改变。

因此：

- 条目式更新函数绑定具体 `ChatMessage` 版本，而不是只绑定 `ChatMessageContainer`；
- 压缩区间仍然可以以静态容器为粒度，但应保存生成时选中的消息版本指纹，用于完整性验证；
- 所有缓存键和摘要来源哈希都必须包含足够的信息，以区分不同分支、不同消息版本和不同定义版本。

### 2.3 不要求模型生成 JSON Patch

其它系统常见的“贫血对象 + 模型生成 JSON Patch”对部分模型正确率较差，尤其容易出现：

- JSON Pointer 路径错误；
- 数组下标错误；
- patch 顺序错误；
- 模型输出解释文本而非合法 patch；
- 对复杂领域方法和约束表达困难。

PulsarAI 不以模型生成 JSON Patch 作为主要更新协议。

模型应基于文档输出普通 JavaScript 函数，或在 Agent 流程中调用插件 interface 明确定义的方法。底层实现可以使用事务、copy-on-write、数据库版本、内部写日志或其它机制，但这些机制不应成为模型必须直接生成的格式。

### 2.4 声明在插件 interface 中的会话内操作属于正常会话语义

在普通而非任务对话中，人物设定、世界状态、JSON 记忆和会话数据库容器的变化，可以被视为会话本身的一部分，而不是应被一概禁止的副作用。

但执行引擎仍然需要区分：

- 可重复读取的纯操作；
- 会话内部、可重放、可分支的状态变更；
- 外部世界中只应执行一次、不能因分支重放而重复发生的操作。

产品语义上可以不把第二类称为副作用，但运行时必须知道它是否可重放。

## 3. 条目式记忆

### 3.1 目标

条目式记忆用于实现：

- 随剧情发展修改人物设定；
- 保存角色关系、地点、物品、状态和世界事实；
- 让 Agent 操作一个 JSON 状态；
- 让 Agent 操作一个未来实现的数据库容器；
- 将插件 interface 中明确定义的会话内操作绑定到消息版本；
- 切换消息版本或分支后，自动得到该路径对应的正确状态；
- 不要求每条消息都持久化完整对象快照。

### 3.2 更新函数绑定具体消息版本

更新定义属于具体 `ChatMessage`，可以概念化为：

```ts
interface VariableUpdate {
  source: string;
  sourceHash: string;
  schemaVersion: number;
  runtimeVersion: string;
}
```

实际字段名尚未定案。它可以位于 `ChatMessage.meta`，也可以是消息拥有的独立记录，但不能只挂在容器上。

模型输出的函数可以类似：

```js
function update(state, api) {
  state.player.damage(10);
  state.inventory.add("potion", 1);
}
```

消息版本切换后，系统选择新版本绑定的更新函数。历史版本及其函数继续保留，不被新版本覆盖。

### 3.3 更新函数是事实来源

如果期望“切换上游分支后，下游更新基于新状态重新计算”，则持久化的应是函数或可重放调用，而不是函数第一次运行产生的最终 patch。

例如：

```js
function update(state) {
  state.hp = Math.floor(state.hp * 0.9);
}
```

在不同上游状态下应产生不同结果。这是动态重放语义。

如果未来某些操作要求“无论上游如何变化，都保持当时记录的固定结果”，那属于另一种历史事件语义，应显式建模为固定命令参数或已记录事件，不能与动态函数重放混为一谈。

### 3.4 状态版本，而不是一个从头修改到尾的对象

沿路径计算时，状态关系应理解为：

```text
S0 --F1--> S1 --F2--> S2 --F3--> S3
```

- `S0` 是初始对象版本；
- `F1` 是第一个选中消息版本绑定的更新函数；
- `S1` 是执行后的新对象版本；
- 每个 `Sᵢ` 都表示一个独立的历史状态；
- 后续函数不能原地修改已经被其它消息视图或分支引用的旧版本。

概念类型：

```ts
interface StateVersion<T> {
  key: string;
  parentKey: string | null;
  messageVersionId: string | null;
  root: T;
}
```

缓存指向的就是某个路径前缀对应的 `StateVersion`。

### 3.5 为什么区间显示需要多个状态版本

用户通常同时看到一段消息，而不是单独看一条消息。

如果界面显示第 40 至 60 条消息，并且消息渲染需要使用其发生时的变量，则至少需要：

```text
before(message 40)
after(message 40)
after(message 41)
...
after(message 60)
```

不能使用一个可变对象从第 40 层一路修改到第 60 层后，再把同一引用交给所有消息组件，否则所有消息最终都会看到第 60 层状态。

正确的区间求值方式是：

1. 找到区间开始前最近的缓存状态版本；
2. 从缓存版本重放到区间入口；
3. 对可见区间内的更新逐条产生新状态版本；
4. 把对应版本交给消息渲染；
5. 当区间离开虚拟列表和 overscan 范围后，由 LRU 决定是否释放。

需要在实现前明确消息读取的是：

- 更新执行前的 `before` 状态；
- 更新执行后的 `after` 状态；
- 或某种用户/助手回合边界状态。

这个边界不能靠调用方猜测。

### 3.6 状态版本的四种实现方式

#### 3.6.1 完整深拷贝

```js
const next = structuredClone(previous);
update(next);
```

优点：

- 语义简单；
- 每个版本天然独立；
- 易于实现区间状态。

缺点：

- 时间和内存近似为“消息数 × 对象大小”；
- `structuredClone` 不适合函数、闭包、Proxy、自定义类语义和某些原生资源；
- 大对象只修改少量字段时浪费明显。

#### 3.6.2 自定义 `fork`

对象定义提供自己的版本分叉能力：

```ts
interface VersionedVariableObject<T> {
  fork(source: T): T;
}
```

优点：

- 能保留自定义类和富对象语义；
- 对象作者可以针对内部结构优化；
- 不强制通用运行时理解对象内部。

缺点：

- 对象作者必须正确实现；
- 错误的浅拷贝仍然会污染分支；
- 不同插件可能具有不同性能特征。

#### 3.6.3 Copy-on-write / 持久化数据结构

新版本只复制被修改路径，未修改节点与父版本共享。

如果一个大状态只修改一个角色的 HP，新版本只需增加新的根路径和被修改节点，而不必复制完整状态。

大致空间复杂度从：

```text
O(版本数 × 完整状态大小)
```

降为：

```text
O(初始状态大小 + 所有版本实际修改路径的总大小)
```

它仍然属于 clone，只是使用结构共享的增量 clone。

#### 3.6.4 序列化 / 反序列化

序列化主要用于：

- 把状态检查点写入数据库；
- 应用重启后恢复缓存；
- 把状态发送给 Worker；
- 跨设备同步；
- 版本管理和导出。

纯内存状态版本缓存不必强制序列化。

如果完全任意的富对象既不提供 `fork`，又无法进入持久化结构，也不允许序列化，那么运行时无法同时安全保存多个历史对象版本。此时只能对每个目标位置从初始状态重新重放，或放弃同时展示多个历史状态。

### 3.7 推荐的对象边界

推荐把“模型看到的富对象”和“版本系统管理的状态内核”分开，但不要求模型面对贫血对象。

概念接口：

```ts
interface VariableDefinition<TData, TFacade> {
  revision: string;
  schemaVersion: number;

  createInitial(): TData;

  beginVersion(parent: TData): {
    facade: TFacade;
    commit(): TData;
    rollback(): void;
  };
}
```

模型看到的可以是：

```js
function update(state) {
  const alice = state.characters.get("alice");
  alice.damage(10);
  alice.addStatus("poisoned");
}
```

底层 facade 可以把方法调用映射到 copy-on-write draft 或数据库事务。

这种结构的目标不是要求模型生成 patch，而是：

- 让模型使用自然的领域方法；
- 让定义作者隐藏真实数据结构；
- 让版本系统能安全创建下一个状态；
- 让迁移尽量发生在 facade 和定义内部；
- 让数组、Map、Set 和数据库操作使用稳定语义。

### 3.8 “规范操作”或内部写日志

此前讨论中的“规范操作”是运行时执行函数后可选记录的内部变更日志，不是模型输出协议。

例如模型调用：

```js
state.characters.get("alice").damage(10);
```

运行时内部可能记录：

```ts
[
  {
    kind: "write",
    path: ["characters", "alice", "hp"],
    before: 80,
    after: 70,
  },
]
```

它可能用于：

- 事务失败后的回滚；
- copy-on-write 路径识别；
- 变量变化差异显示；
- 撤销和调试；
- 性能分析；
- 缓存失效辅助；
- 未来的数据迁移工具。

内部写日志不是第一版成立的必要条件。

如果 `beginVersion/commit/rollback` 或数据库事务已经可靠提供这些能力，第一版可以不额外保存独立写日志。

### 3.9 编译缓存和状态缓存

条目式记忆至少存在两类不同缓存：

#### 编译缓存

```text
sourceHash → compiledFunction
```

它避免每次重放都重新调用 `new Function` 编译同一源码。

#### 状态版本缓存

```text
chainKey → StateVersion
```

状态版本键可以概念化为：

```text
rootKey = hash(
  variableDefinitionRevision,
  initialStateRevision,
  schemaVersion,
  runtimeVersion
)

nextKey = hash(
  parentKey,
  containerId,
  selectedMessageVersionId,
  updateSourceHash
)
```

切换消息版本、切换分支、修改函数或升级定义后，后续键自然变化。新路径不会错误命中旧状态，也不需要逐个扫描并删除所有后代缓存。

缓存应为内存 LRU，并设置容量约束，例如：

- 最大状态版本数量；
- 最大估算内存；
- 当前路径尾部固定保留；
- 当前可见区间和 overscan 固定保留；
- 最近访问分支头固定保留；
- 其它版本按 LRU 释放。

第一版不需要把每个状态版本持久化到数据库。

### 3.10 区间求值

请求激活路径上 `[from, to]` 区间状态时：

1. 为路径构建有序的消息版本和更新函数序列；
2. 寻找不晚于 `from` 的最深缓存前缀；
3. 从该状态版本重放到 `from`；
4. 对 `[from, to]` 逐条创建状态版本；
5. 返回区间需要的 `before/after` 状态；
6. 缓存尾部版本和当前可见版本；
7. 释放不再需要的中间版本。

当切换分支时，寻找新旧路径的最长公共前缀。公共前缀状态可以复用，只重放变化的后缀。

### 3.11 确定性与执行限制

动态重放要求更新函数具有确定性。

默认应限制：

- 不直接访问网络和文件；
- 不创建不可撤销的外部动作；
- 不直接使用真实 `Date.now()`；
- 不直接使用非确定性的 `Math.random()`；
- 不使用计时器；
- 不产生脱离事务的后台异步任务；
- 不修改状态定义、方法表或 Sandbox 全局环境；
- 不保存指向旧 draft 的引用供以后使用。

如果需要时间或随机数，应由运行时通过 `api` 注入，并由消息版本 ID、会话 ID或记录值保证重放稳定。

每条更新应：

- 在事务中执行；
- 成功后一次性提交；
- 失败后完整回滚；
- 有执行时间、递归深度或操作次数限制；
- 保存足够诊断信息指向所属消息版本。

默认错误策略建议为：

- 保留最后一个有效状态；
- 标记失败的消息版本；
- 停止继续重放依赖该状态的后续更新；
- 返回 `blockedAtMessageId` 和诊断；
- 由用户修复、切换版本或显式跳过后重新计算。

是否允许“失败后忽略该更新并继续”需要作为明确策略，而不是隐式行为。

### 3.12 插件 interface 与重放分类

插件暴露给会话的操作可概念化为：

```ts
type ReplayMode =
  | "pure"
  | "conversation-state"
  | "external-once";
```

#### `pure`

- 只读取；
- 可以重复执行；
- 不改变会话内外状态。

#### `conversation-state`

- 修改人物、世界、JSON 或会话数据库容器；
- 属于普通会话语义；
- 必须被绑定到具体消息版本；
- 必须能在分支切换、重新打开和重放时恢复；
- 可以通过函数、事务调用日志或数据库版本实现。

#### `external-once`

- 发送邮件；
- 支付；
- 发布内容；
- 修改真实外部文件；
- 调用具有不可重复外部影响的服务。

这一类即使明确写在插件 interface 中，也不能在状态重放时再次执行。它需要幂等 ID、外部执行记录和单独的确认/恢复语义。

如果主 Agent 在生成中调用 `conversation-state` 操作，这些操作必须被记录到当前生成目标的消息版本中。不能只修改当前内存对象，否则切换分支或重启后无法还原。

同一消息版本内的多个操作应保持原始顺序，并作为同一事务或明确的事务序列处理。

### 3.13 JSON 状态和数据库容器

#### JSON 状态

JSON 状态可以使用：

- copy-on-write draft；
- 持久化对象结构；
- 自定义 `fork`；
- 小对象时直接深拷贝。

#### 数据库容器

数据库容器不能让重放函数直接反复修改一个全局可变数据库。

更合适的是分支感知的 MVCC 或覆盖层：

```text
DatabaseStateVersion
  ├─ parentVersion
  ├─ transaction log / changed records
  └─ root handle
```

每条消息的事务从父版本创建子版本。切换分支选择另一条版本链，公共前缀版本可以共享。

数据库版本句柄可以作为 `StateVersion.root`，不需要 clone 完整数据库。

数据库容器的具体实现、SurrealDB 集成方式和持久化生命周期仍待执行计划决定。

### 3.14 数组和集合

直接把数组数字下标作为稳定领域身份会导致：

- `splice` 后所有后续下标改变；
- 排序后历史路径含义改变；
- 从尾部查询受影响键难以判断；
- 迁移和合并困难。

对需要长期引用和版本追踪的集合，建议内部规范化为：

```ts
interface NormalizedList<T> {
  byId: Record<string, T>;
  order: string[];
}
```

模型仍可通过富对象方法操作：

```js
state.inventory.insertAfter(anchorId, item);
state.inventory.remove(itemId);
state.inventory.move(itemId, anchorId);
```

对于简单、短小、整体替换的数组，不必强制规范化。数组策略应由变量定义决定。

原生 `Map`、`Set`、类私有字段和复杂原型需要专门的版本适配。普通 Proxy 不能可靠观察所有自定义类内部变化，尤其无法通用捕获 `#private` 字段。

### 3.15 Schema 与迁移

每条更新函数应记录它面向的：

- `schemaVersion`；
- `runtimeVersion`；
- 变量定义 revision。

迁移策略可能包括：

1. 旧函数在旧 facade 上运行，得到旧状态后再迁移；
2. 新 facade 为旧字段和旧方法提供兼容层；
3. 显式迁移旧函数；
4. 将稳定领域方法保持兼容，只迁移底层数据；
5. 对不兼容更新标记阻塞，要求用户处理。

模型直接访问内部路径越多，迁移越困难；模型通过稳定领域方法操作，迁移越容易。

不能让旧函数在未知的新结构上静默运行。

### 3.16 关于布隆过滤器和从尾部查键

“贫血对象 + 从尾部查最后一个受影响键”只适合近似如下的独立末值覆盖：

```js
state.name = "Alice";
state.gold = 100;
```

一旦更新包含：

```js
state.gold += 10;
state.maxHp = state.level * 20;
state.total = state.items.reduce(...);
if (state.flags.vip) state.discount = 0.8;
```

系统不仅要知道谁写了一个键，还要知道：

- 更新读取了哪些键；
- 是否依赖旧值；
- 控制流依赖哪些键；
- 是否枚举整个对象；
- 数组结构是否发生变化；
- 方法内部修改了什么。

布隆过滤器只能回答“某个区段可能写过某个键”，存在假阳性，也不能证明某个函数可以安全跳过。

在会话规模下，每个区块保存精确 `Set<string>`、位图或路径 Trie 往往比布隆过滤器简单可靠。

布隆过滤器可以作为未来针对超大日志和单键查询的辅助索引，但不应成为条目式记忆的主要计算模型，也不能代替状态版本。

### 3.17 性能数量级

讨论期间在当前机器的 Node/V8 环境做过非产品基准测试，仅用于数量级判断：

| 工作量 | 约耗时 |
| --- | ---: |
| 编译并执行 1,000 条不同的简单函数 | 38 ms |
| 编译并执行 5,000 条不同的简单函数 | 260–310 ms |
| 编译并执行 10,000 条不同的简单函数 | 424–438 ms |
| 执行已编译的 10,000 条简单函数 | 约 3 ms |
| 每层扫描 10,000 个数组项，共 1,000 层 | 约 53 ms |
| 每层扫描 10,000 个数组项，共 10,000 层 | 约 509 ms |
| Proxy 记录每层两次写入，共 1,000,000 层 | 约 388 ms |

结论：

- 编译缓存非常重要；
- 简单、已编译、常数复杂度更新在正常会话规模下不会成为瓶颈；
- 每层遍历大型数组会比楼层数量本身更早成为瓶颈；
- 任意函数可能单条就超过一秒，因此必须有执行预算；
- 产品体验不应以一秒为优化线，冷重放超过约 50–100 ms 就可能需要后台计算、更多内存前缀缓存或稀疏持久检查点；
- Tauri WebView、实际 Sandbox、真实对象、数据库和移动设备的结果可能与该基准有明显差异，最终阈值必须实测。

## 4. 压缩式记忆

### 4.1 目标

压缩式记忆用于：

- 应对持续增长的会话上下文；
- 将连续若干消息总结成一条记忆；
- 以静态消息容器为压缩粒度；
- 允许摘要引用摘要，形成分级压缩；
- 读取时沿激活路径使用摘要跳过被覆盖的原始消息；
- 根据选中的摘要生成临时模型消息；
- 允许以后由模型主动向下搜索特定区间；
- 支持固定分块、语义分割和并行模型调用；
- 不删除原始消息，不让摘要成为唯一事实来源。

### 4.2 为什么以容器为压缩粒度

压缩触发时，被覆盖的历史容器应当已经静态：

- 不再生成；
- 不再编辑；
- 不再添加消息版本；
- 不再改变其内部选中的消息版本；
- 不再改变被压缩区间的拓扑。

在此前提下，以容器而非消息版本作为主要区间粒度是合理的。

不过摘要仍应保存当时选中的消息版本 ID 或内容哈希，以防备：

- 数据恢复；
- 旧版本导入；
- 非预期编辑；
- 迁移错误；
- 静态约束被绕过。

这属于来源完整性校验，不改变“容器是压缩粒度”的产品语义。

“静态容器”是否需要显式 `sealed` 状态，以及冻结后允许哪些恢复操作，仍待确定。

### 4.3 压缩段，而不是混入分支指针

不建议把记忆跳跃直接写入：

- `previousContainer`；
- `availableNextContainer`；
- `activeNextContainer`。

这些字段表达真实会话拓扑。压缩关系是派生索引，二者混合会带来：

- 分支遍历与摘要遍历相互干扰；
- 难以防止循环；
- 删除或重建摘要时需要修改会话容器；
- 难以同时保留多个压缩层级；
- 难以表达同一区间的不同摘要版本。

压缩指针应建模为 Conversation Feature 拥有的独立不可变区间记录。

概念类型：

```ts
interface CompressedMemorySegment {
  id: string;
  conversationId: string;

  level: number;

  startContainerId: string;
  endContainerId: string;

  source:
    | {
        kind: "containers";
        containerIds: string[];
        messageVersionIds: string[];
      }
    | {
        kind: "segments";
        segmentIds: string[];
      };

  sourceHash: string;
  content: string;

  leafContainerCount: number;
  sourceTokenCount: number;
  compressedTokenCount: number;

  status: "pending" | "ready" | "failed" | "superseded";

  compressorId: string;
  compressorVersion: string;
  createdAt: string;
}
```

字段名和持久化形态尚未定案。

### 4.4 分级区间树 / DAG

最低层摘要直接引用原始容器：

```text
M1 = summary(C1 ... C16)
M2 = summary(C17 ... C32)
M3 = summary(C33 ... C48)
```

更高层摘要引用相邻的低层摘要：

```text
M4 = summary(M1, M2, M3)
```

结构：

```text
                    M4: C1-C48
                  /      |      \
           M1: C1-C16 M2: C17-C32 M3: C33-C48
```

这就是“指针本身也可以包含指针”。

为了保持可解释性，结构应满足：

- 子区间有序；
- 子区间连续；
- 子区间不重叠；
- 父区间等于所有子区间的有序并集；
- 父节点只能引用已经完成的子节点；
- 子节点不能引用父节点；
- 不允许循环；
- 节点生成后不可原地改写来源或内容；
- 重新压缩产生新节点，旧节点可以标记为 `superseded`。

在单条路径上它接近一棵树。因为不同分支可以复用完全相同的静态公共前缀摘要，整体存储上也可以形成无环共享 DAG。

### 4.5 不在每一层重复保存所有容器 ID

如果每个高层摘要都重复列出其覆盖的所有原始容器，存储可能接近 `O(N log N)`。

更合适的是：

- 叶摘要保存容器 ID 和消息版本指纹；
- 父摘要只保存子摘要 ID；
- 所有层保存首尾容器、叶子数量、token 数量和来源哈希；
- 需要下探时递归展开来源。

例如：

```text
M1.source = [C1 ... C16]
M2.source = [C17 ... C32]

M3.source = [M1, M2]
M3.start = C1
M3.end = C32
M3.leafContainerCount = 32
M3.sourceHash = hash(M1.sourceHash, M2.sourceHash)
```

这使高层结构接近 Merkle DAG：

- 子摘要变化会产生新的子哈希；
- 父摘要来源哈希随之变化；
- 旧父摘要不会错误覆盖新来源；
- 可以快速验证摘要是否仍匹配当前激活路径。

### 4.6 分级压缩

可以把压缩层级理解为：

- L0：原始消息容器；
- L1：原始容器区间摘要；
- L2：若干相邻 L1 摘要的摘要；
- L3：若干相邻 L2 摘要的摘要；
- 以此类推。

“一次压缩不够，再次压缩落在上一次压缩指针上”对应父摘要引用子摘要。

升级机制可以类似 LSM Tree 的分层合并：

```text
4 × L1 → 1 × L2
4 × L2 → 1 × L3
```

也可以基于 token 而非固定节点数：

```text
连续未压缩来源达到一定 token → L1
连续 L1 摘要达到一定 token → L2
连续 L2 摘要达到一定 token → L3
```

具体层级倍率和 token 阈值由执行计划及实测决定。

高层摘要生成期间不应阻塞会话读取。父摘要未就绪时，读取器继续使用其子摘要或原始消息。

### 4.7 压缩触发

触发条件不应只依赖消息数量。更合理的输入包括：

- 被封存历史的原始 token 数；
- 当前上下文窗口和预留输出 token；
- 最近原始消息保留窗口；
- 同级可合并摘要数量；
- 后台任务并发预算；
- 当前设备性能与网络状态；
- 用户是否启用自动压缩。

第一版可以使用固定 token 分块并对齐完整 user/assistant 回合。

语义分割可以后续加入，用于避免在一个事件或主题中间切开，但它本身需要计算成本和稳定性设计。

### 4.8 并行压缩

互不重叠的叶区间可以并行调用模型：

```text
C1-C16  → M1
C17-C32 → M2
C33-C48 → M3
```

这些叶摘要完成后，再进行父层归并：

```text
M1 + M2 + M3 → M4
```

为了减轻固定分块造成的边界信息丢失，可以给压缩模型提供少量前后边界消息作为理解上下文，但：

- 边界消息不计入该摘要的覆盖范围；
- 不应因此产生重叠覆盖；
- 摘要应明确只总结自己的正式来源区间。

压缩任务应以 `sourceHash` 作为幂等身份。相同来源和相同压缩器版本不应重复创建等价任务。

如果任务完成时来源路径已经变化，应丢弃或保留为非当前摘要，不能直接加入当前读取索引。

### 4.9 “最远跳跃”与摘要前沿

原始想法是在读取激活路径时，从当前位置寻找覆盖最远的可用指针；如果该指针又被更高层指针覆盖，则继续寻址到最高层。

这种方式可以得到最高压缩率，但无条件使用最高层摘要可能产生过度压缩。

例如整个 3,000 条消息已经有一条 500 token 的顶层摘要，而当前模型上下文仍有大量空间。此时只使用顶层摘要会不必要地损失细节。

更通用的读取模型是“摘要前沿”：

- 前沿由若干互不重叠的原始区间或摘要节点组成；
- 它们按时间顺序覆盖目标历史区间；
- 每个原始容器只被覆盖一次；
- 高层节点可以被替换为其子节点，实现下探；
- 多个相邻子节点也可以被父节点替换，实现上卷压缩。

例如：

```text
[M4]
```

可以展开成：

```text
[M1, M2, M3]
```

还可以只展开相关部分：

```text
[M1, raw(C17-C32), M3]
```

“一直跳到最顶部”可以作为最紧张上下文预算下的默认选择，但读取接口不应只能返回最高层节点。

### 4.10 前沿选择策略

可能的默认策略：

1. 最近若干轮或最近若干 token 始终保留原始消息；
2. 对更早历史先选择尽可能高层的摘要；
3. 在剩余 token 预算内展开较近的摘要；
4. 优先展开与当前用户请求语义相关的摘要；
5. 对标记为高重要度或存在未完成事项的区间保留更高分辨率；
6. 保证最终前沿按时间顺序、无重叠、无重复覆盖；
7. 无有效摘要时回退原始消息。

第一版可以保持简单：

- 近期原文；
- 旧历史最高可用摘要；
- 固定 token 预算；
- 暂不做语义相关性重排。

### 4.11 激活路径验证和跳跃索引

读取时先建立：

```text
containerId → activePathIndex
```

压缩段只有在以下条件成立时才可使用：

- `startContainerId` 和 `endContainerId` 都位于当前激活路径；
- 开始位置不晚于结束位置；
- 所有来源容器或子摘要与该路径连续匹配；
- 选中的消息版本指纹匹配；
- `sourceHash` 匹配；
- 状态为 `ready`；
- 压缩器版本未被强制淘汰。

可以建立：

```text
startContainerId → 可从此处开始的压缩段列表
```

并按覆盖终点、层级或 token 收益排序。

读取器不必每次递归寻找“指针中的指针”才能知道跳多远，因为每个压缩段已经保存扁平的首尾容器和叶子数量。子引用主要用于验证和下探。

### 4.12 临时记忆消息

压缩摘要覆盖了多个用户和助手消息，不应伪装成某一个真实用户消息或助手消息。

内部可以使用专用类型：

```ts
interface MemoryContextMessage {
  role: "memory";
  segmentId: string;
  startContainerId: string;
  endContainerId: string;
  level: number;
  content: string;
}
```

它只存在于上下文编译阶段，不写回原始会话。

在模型连接边界，根据供应商能力转换为安全格式，例如合并到一条 system memory block：

```xml
<conversation_memory
  segment="M4"
  range="C1..C48"
  level="2"
>
...
</conversation_memory>
```

需要避免：

- 让模型误以为摘要是某个助手曾经说过的原话；
- 把内部 ID 当作自然语言事实；
- 将临时摘要持久化为普通聊天消息；
- 在不同供应商不支持中间 system 消息时生成非法消息序列。

具体 provider-safe 编译方式由生成上下文实现决定。

### 4.13 摘要内容

压缩摘要是有损的。递归摘要会产生“原文 → L1 → L2 → L3”的误差累积。

摘要至少应考虑保留：

- 已确认事实；
- 人物、关系、地点和物品变化；
- 关键事件及因果关系；
- 用户意图；
- 未完成目标；
- 承诺和约束；
- 冲突、不确定性和待确认信息；
- 后续可能需要下探的来源区间提示。

条目式记忆已经负责当前结构化世界状态，因此压缩式记忆不必重复承担全部变量数据库职责。它可以更专注于：

- 事件经过；
- 对话语义；
- 因果链；
- 角色动机；
- 未解决话题。

是否使用纯文本摘要，或增加少量结构化字段，仍待确定。应避免为了结构化而再次要求模型输出复杂、脆弱的大型 JSON。

### 4.14 下探和主动检索

分级摘要天然支持从粗到细的检索。

第一版不需要让每个指针保存自己的可执行下探函数。可以提供固定能力：

```ts
memory.getSegment(segmentId)
memory.listChildren(segmentId)
memory.expand(segmentId, depth?)
memory.readRange(startContainerId, endContainerId)
memory.search(query, options?)
```

主动记忆提取可以按以下方式工作：

```text
读取高层摘要
  → 找到与问题相关的摘要段
  → 展开其子段
  → 必要时读取叶段或原始容器
  → 返回带来源区间的结果
```

未来可以把这些固定能力包装为一个记忆提取 Agent。

第一阶段保持简单：

- 固定树遍历；
- 明确的区间读取；
- 不引入每段自定义代码；
- 不强制引入向量数据库；
- 不建立独立的第二套会话生成路径。

### 4.15 分支与公共前缀

压缩段绑定精确的静态来源路径。

如果两个分支共享完全相同且已封存的公共前缀，则该前缀摘要可以共享。

分支开始后的区间分别生成自己的摘要：

```text
共同前缀摘要 P
       /       \
  分支摘要 A   分支摘要 B
```

读取当前分支时只选择：

- 与公共前缀匹配的共享摘要；
- 与当前后续分支匹配的摘要。

不能因为首尾容器看似接近，就让摘要跨过不属于同一激活路径的分支。

### 4.16 摘要失效与重建

即使设计上只压缩静态容器，仍应定义失效机制。

导致摘要失效的情况包括：

- 来源容器内容变化；
- 选中的消息版本变化；
- 来源区间拓扑变化；
- 子摘要被新版本替代；
- 压缩器提示、模型或版本发生强制升级；
- 数据恢复产生来源哈希不一致；
- 摘要记录损坏。

处理方式：

- 不原地修改摘要；
- 创建新的摘要节点；
- 旧节点标记为 `superseded` 或保持为历史记录；
- 当前索引只选择来源匹配的新节点；
- 父节点的 `sourceHash` 不匹配时自动停止使用；
- 后台按需要重新生成父层。

### 4.17 压缩失败

压缩是派生后台任务，失败不应破坏原始会话。

失败时：

- 保存诊断和来源范围；
- 继续使用原始消息或低层摘要；
- 不创建半有效跳跃；
- 允许重试；
- 相同来源和压缩器版本避免并发重复任务；
- 父层不能引用失败或未完成的子层。

## 5. 两类记忆的组合

### 5.1 生成顺序

推荐的完整生成顺序：

1. 创建空白助手目标消息和容器；
2. 固定本次生成所使用的 Conversation、激活路径和消息版本 revision；
3. 构建最小授权运行环境；
4. 解析 Plugin 容器和显式引用；
5. 沿完整激活路径求值条目式记忆，得到当前状态版本；
6. 根据上下文预算选择压缩式记忆前沿；
7. 将摘要前沿编译为临时 memory context；
8. 保留近期或未被摘要覆盖的原始消息；
9. 把当前条目式状态或其受控 facade 加入生成环境；
10. 编译根 `context.imd`；
11. 交给 `agentprocess/index.js` 和其中显式创建的 Agent；
12. Agent 生成文本并调用已授权插件 interface；
13. 将会话内状态变更记录到当前消息版本；
14. 完成消息内容、生成元数据和状态更新函数的持久化；
15. 事务执行该更新，产生新的尾部状态版本缓存；
16. 生成结束后，在后台判断是否出现新的可封存、可压缩区间。

具体步骤应与现有固定启动序列对齐。记忆功能不应创建平行生成路径。

### 5.2 压缩跳跃不能跳过状态更新

假设一个摘要覆盖：

```text
C1 ... C100
```

生成上下文可以用一条摘要临时消息替代这些容器的原始文本。

但条目式记忆仍然必须得到：

```text
S100 = F100(...F2(F1(S0)))
```

它可以使用 `C1...C96` 的缓存状态版本，只重放后四层，但不能因为文本被摘要覆盖就假设状态也包含在摘要中。

压缩式记忆是文本上下文优化，不是状态计算检查点。

如果未来摘要节点同时引用一个状态版本缓存或持久状态检查点，那只是额外加速索引，不能改变两种记忆的事实来源。

### 5.3 可以共享的身份信息

两类记忆可以共享：

- Conversation ID；
- 容器 ID；
- 选中消息版本 ID；
- 激活路径 revision；
- 来源内容哈希；
- Plugin/变量定义 revision；
- 创建时间和诊断。

但它们的缓存和遍历应分离：

- 条目式记忆按状态版本链重放；
- 压缩式记忆按摘要区间前沿选择；
- 条目状态缓存失效不必删除摘要；
- 摘要失效不必删除状态版本；
- 原始消息变化会通过各自来源哈希分别使二者失效。

### 5.4 生成期间路径变化

如果用户在压缩任务或生成任务运行期间切换分支、切换消息版本或编辑来源：

- 当前任务继续使用开始时冻结的路径 revision；
- 结果持久化前验证目标消息和来源 revision；
- 不匹配时不得把结果直接接到新的激活路径；
- 压缩任务可以保留为旧分支摘要或丢弃；
- 条目式更新只能绑定到本次生成实际创建的消息版本；
- 不能把旧路径计算出的状态缓存错误标记为新路径尾部。

## 6. 存储边界

### 6.1 必须持久化

条目式记忆：

- 消息版本绑定的函数或可重放调用；
- 源码/调用哈希；
- schema/runtime/定义版本；
- 执行诊断和状态；
- 外部一次性操作的幂等执行记录；
- 数据库容器本身需要的版本日志或根句柄。

压缩式记忆：

- 摘要节点；
- 来源容器或子摘要引用；
- 首尾容器；
- 来源哈希；
- 摘要内容；
- 层级、token 和状态元数据；
- 压缩器身份与版本。

### 6.2 默认只存在内存

- 编译后的 JavaScript 函数；
- 热路径状态版本；
- 可见区间状态版本；
- 当前分支头状态；
- 内部 draft；
- 临时写日志；
- 摘要前沿；
- provider-safe 临时 memory 消息；
- 激活路径位置索引。

### 6.3 可选持久化缓存

如果冷重放性能不足，未来可以增加：

- 稀疏状态检查点；
- 数据库 MVCC 根；
- 序列化状态版本；
- 热门分支尾部状态；
- 已编译代码的安全中间表示；
- 摘要检索索引。

这些都是派生缓存，必须可以从事实来源重建。

### 6.4 备份、导入和同步

记忆数据参与备份时需要区分：

- 会话事实来源；
- 可重建派生缓存；
- 外部一次性操作记录。

建议：

- 更新函数和摘要节点随 Conversation 导出；
- 纯内存状态不导出；
- 可序列化状态检查点可以选择不导出，导入后重算；
- 外部操作记录必须保留，避免导入或恢复后重复执行；
- 稳定 ID 和来源哈希在导入合并时保持可验证；
- 如果消息和摘要发生冲突，原始消息版本优先，摘要重新生成。

具体 Backup 集成由执行计划决定。

## 7. 可观察性与用户体验

记忆系统不应完全隐藏。

未来可能需要显示：

- 某条消息是否产生了条目式记忆更新；
- 更新函数是否执行成功；
- 当前状态从哪个消息版本计算而来；
- 哪个状态版本阻塞了后续重放；
- 当前上下文哪些区间使用了摘要；
- 摘要覆盖范围和压缩层级；
- 摘要是否可以展开；
- 摘要来源是否已失效；
- 后台压缩任务状态；
- 最近一次压缩或重放耗时。

`todo/main.todo` 已有“类似 Codex 的记忆更新小提示框”方向。具体 UI 不在本文定案。

需要避免：

- 把大量内部技术信息永久塞进每条消息；
- 让后台压缩打断聊天；
- 让摘要失败表现成生成失败；
- 让状态重放失败静默产生错误世界状态；
- 让用户无法判断当前看到的是原文、摘要还是派生状态。

## 8. 性能目标

建议把性能预算分为：

### 热路径

- 当前路径尾部已有状态缓存；
- 新消息只执行一个或少量新更新；
- 应尽量低于一帧或至少低于可感知延迟；
- 不应重新编译全部历史函数。

### 分支切换

- 复用最长公共前缀；
- 只重放变化后缀；
- 状态和摘要索引都按新激活路径验证；
- 可见区间优先，非可见历史可延迟物化。

### 冷打开

- 从初始状态完全重放；
- 可以先计算当前尾部，再按虚拟列表需求计算历史可见区间；
- 超过约 50–100 ms 时应记录指标；
- 达到明显延迟后再考虑稀疏持久检查点。

### 压缩

- 后台执行；
- 叶区间可并行；
- 父层等待子层；
- 不阻塞读取；
- 有并发和费用预算；
- 相同来源任务幂等去重。

## 9. 不采用或暂不采用的方案

### 不采用模型直接生成 JSON Patch 作为主要协议

原因：

- 部分模型正确率不足；
- 复杂路径和数组容易出错；
- 不适合表达领域方法；
- schema 迁移脆弱；
- 用户已经明确认为类似项目中的实现体验很差。

### 不用布隆过滤器替代状态版本

原因：

- 只能做可能性索引；
- 无法处理读取依赖和条件控制流；
- 数组、枚举和封装方法不友好；
- 不能提供同时存在的历史对象版本。

### 不把摘要写成普通聊天消息

原因：

- 摘要是派生内容；
- 可能被重建；
- 不属于用户或助手原始发言；
- 会污染消息版本和分支语义。

### 不把摘要边混入 Conversation 分支边

原因：

- 会话拓扑与压缩索引职责不同；
- 多层摘要和失效重建会污染容器结构；
- 容易产生循环和错误导航。

### 暂不为每个摘要保存自定义下探函数

原因：

- 固定区间 API 已经足够；
- 每段代码增加安全、迁移和权限复杂度；
- 记忆提取 Agent 可以后续建立在固定能力上。

### 暂不强制向量数据库

摘要树本身已经提供从高层到原文的搜索空间裁剪。语义索引可以在确认需要后作为辅助，而不是成为第一版前提。

## 10. 已形成的设计共识

1. 记忆分为条目式记忆和压缩式记忆。
2. 条目式记忆的更新函数绑定具体消息版本。
3. 更新函数沿当前激活路径重放。
4. 状态缓存是路径前缀对应的对象版本，不是一个共享可变对象。
5. 同时显示消息区间时，需要对应的多个历史状态版本。
6. 多版本对象必须通过深拷贝、自定义 `fork`、copy-on-write、持久化结构或数据库版本实现，不存在无需版本化能力的通用捷径。
7. 不要求模型生成 JSON Patch。
8. 模型可以使用富对象和插件 interface，底层版本实现对模型透明。
9. 内部变更日志是可选引擎细节，不是模型协议。
10. 编译缓存与状态版本缓存相互独立。
11. 状态缓存键必须包含路径前缀、消息版本和定义版本。
12. 会话内状态操作可以作为普通聊天语义，但必须可重放并绑定消息版本。
13. 外部一次性操作不能在重放时重复执行。
14. 压缩式记忆以静态容器区间为主要粒度。
15. 摘要节点是独立、不可变的区间记录，不混入 Conversation 分支指针。
16. 叶摘要引用原始容器，父摘要引用子摘要。
17. 摘要结构必须无环、连续、有序、无重叠。
18. 父摘要不重复保存所有叶容器 ID，通过子引用、首尾范围和来源哈希验证。
19. 压缩可以按 token 分块并并行生成叶摘要。
20. 更高层摘要由相邻低层摘要再次压缩形成。
21. 读取时使用不重叠的摘要前沿，而不是只能使用最高层摘要。
22. 近期消息应保留原文，旧历史根据预算选择摘要层级。
23. 摘要只替换模型上下文中的文本，不跳过条目式状态计算。
24. 摘要生成临时 memory context，不写回普通消息。
25. 下探优先使用固定区间和子节点接口。
26. 两类记忆共享来源身份和哈希，但保持独立缓存和遍历语义。
27. 所有派生状态和摘要都必须能从事实来源验证或重建。

## 11. 待执行计划决定的问题

以下内容尚未定案：

### Feature 和文件归属

- 是否新增独立 Memory Feature；
- 条目式记忆与压缩式记忆是否同属一个 Feature 的不同子域；
- 与 Conversation、Plugin、Sandbox、Database、Backup 的具体依赖方向；
- `capabilities.ts` 暴露哪些操作。

### 条目式记忆定义

- 初始对象由插件、角色包、Conversation 还是独立资源提供；
- 文档放在插件的哪个约定文件；
- 更新函数字段挂在 `ChatMessage.meta` 还是独立表；
- 更新由主 Agent 主动调用 interface 产生，还是额外模型提取流程产生；
- 同一消息允许一个函数、多个函数还是有序调用记录；
- `before/after` 状态与消息、回合的确切边界；
- 失败后停止、跳过或允许用户选择的策略；
- 是否第一版就实现内部写日志；
- 第一版使用深拷贝、copy-on-write、自定义 `fork` 还是组合适配器；
- JSON 容器与数据库容器是否共享统一事务接口；
- 数据库版本如何映射 SurrealDB；
- 状态对象是否允许 async 方法；
- 执行预算和超时。

### Schema 和迁移

- 变量定义 revision 如何生成；
- 旧 facade 的保留周期；
- 不兼容函数如何提示和修复；
- 状态检查点是否参与迁移；
- 导入旧 Conversation 时如何处理未知定义。

### 压缩式记忆

- 静态容器是否需要显式 `sealed` 字段；
- 容器何时冻结；
- 初级区间 token 大小；
- 每级合并倍率；
- 保留多少近期原始消息；
- 摘要提示词和输出格式；
- 压缩模型和思考深度；
- 自动压缩的费用和并发限制；
- 语义分割何时加入；
- 是否保留边界重叠上下文；
- 摘要重要度、未完成事项和来源引用是否结构化；
- 摘要节点是数据库记录还是 Conversation 附属资源；
- `superseded` 节点保留多久；
- 当前前沿选择算法；
- provider-safe 临时 memory 消息格式；
- 何时加入主动搜索和记忆提取 Agent。

### 缓存和性能

- 状态版本 LRU 的数量和内存限制；
- 是否使用对象大小估算；
- 是否按可见虚拟区间物化历史状态；
- 是否把冷重放移到 Worker；
- Worker 所需序列化边界；
- 何时产生稀疏持久检查点；
- 检查点是否与 Backup 对象仓库复用；
- 移动端的性能和内存预算；
- 压缩任务的后台调度方式。

### UI

- 消息上的记忆更新提示；
- 当前状态查看器；
- 状态差异和错误定位；
- 摘要范围和层级提示；
- 摘要展开/下探入口；
- 自动压缩设置；
- 调试和统计页面；
- 移动端展示回退。

## 12. 预期最终形态

最终，会话上下文可以被理解为三条同时存在的链：

```text
原始会话链：
容器和消息版本构成的激活路径

条目状态链：
初始状态 → 消息更新函数 → 状态版本 → 当前世界状态

压缩摘要链：
原始容器区间 → 叶摘要 → 父摘要 → 上下文摘要前沿
```

一次生成读取：

```text
当前激活消息路径
  + 当前条目状态版本
  + 按 token 预算选择的压缩摘要前沿
  + 未被压缩的近期原始消息
  + Plugin / Capability / Sandbox 环境
```

一次生成完成后写入：

```text
新的助手消息版本
  + 该版本产生的会话内状态更新
  + 新的状态版本缓存
  + 可选的后台压缩候选
```

这个设计保留：

- 消息版本和分支的可追溯性；
- 人物与世界状态的确定性重放；
- 富对象和插件 interface 的灵活性；
- 不依赖模型生成 JSON Patch；
- 不为每条消息持久化完整对象快照；
- 分级压缩和上下文增长控制；
- 从摘要下探到原始消息的能力；
- 未来接入 JSON、数据库容器、记忆提取 Agent 和持久检查点的空间。
